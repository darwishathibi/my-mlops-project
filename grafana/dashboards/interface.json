{
  "uid": "m4-inference",
  "title": "M4 Chip Inference Performance",
  "timezone": "browser",
  "refresh": "5s",
  "time": { "from": "now-15m", "to": "now" },
  "schemaVersion": 39,
  "panels": [
    {
      "type": "timeseries",
      "title": "Tokens / second",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "custom": { "drawStyle": "line", "fillOpacity": 25 } }, "overrides": [] },
      "targets": [
        { "refId": "A", "expr": "tokens_per_second",
          "datasource": { "type": "prometheus", "uid": "prometheus" } }
      ]
    }
  ]
}